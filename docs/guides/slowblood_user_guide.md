## Get Available GPUs on RunPod

- `make rp-info` from root of repo
- `python cli-get-info-runpod.py`
- Loads the access token from .env file and uses the runpod API to query available GPUs and prices 